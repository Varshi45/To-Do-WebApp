<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>Sign-Up</title>
    <link rel="stylesheet" href="./css/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body style="background: linear-gradient(to bottom, #e2eff2, #0d8fe6);height: 100vh;">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %>
            <h6 class="py-4">Login to your Account....</h6>
            <form action="/session" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="py-3">
                    <label for="email">Email:</label>
                    <input type="email" name="email" class="border border-gray-300 text-gray-900 rounded p-2 w-full" required>
                </div>
                <div class="py-3">
                    <label for="password">Password:</label>
                    <input type="password" name="password" class="border border-gray-300 text-gray-900 rounded p-2 w-full" required>
                </div>
                <div class="text-center py-3">
                    <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Sign In</button>
                </div>
            </form>
            
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <div class="flash-message error">
                    <ul>
                        <% for(var i=0; i<messages.error.length; i++) { %>
                            <li class="Error-Message"><%= messages.error[i] %></li>
                        <% } %>
                    </ul>
                </div>
            <% } %>

            <script>
                document.addEventListener('DOMContentLoaded', function() {
                    const errorMessages = document.querySelector('.flash-message.error');
                    if (errorMessages && errorMessages.querySelector('li')) {
                        errorMessages.style.display = 'block';
                    }
                });
            </script>
        </div>
    </div>
</body>
</html>
